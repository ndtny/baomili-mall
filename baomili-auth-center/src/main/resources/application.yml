server:
  port: 8811

spring:
  application:
    name: baomili-auth-center
  datasource:
    url: jdbc:mysql://101.132.155.185:3306/baomili_mall?serverTimezone=UTC&useSSL=false&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: 123456

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
  configuration:
    auto-mapping-behavior: partial
    map-underscore-to-camel-case: true

logging:
  level:
    root: info
    com.baomili: debug
